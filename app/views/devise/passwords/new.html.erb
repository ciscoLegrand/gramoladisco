<%= render UI::Modal.new do %>
  <section class="relative py-3 w-full px-4 sm:px-0">
		<div class="absolute inset-0 bg-gradient-to-r from-blue-300 to-blue-600 shadow-lg transform -skew-y-6 sm:skew-y-0 sm:-rotate-6 sm:rounded-3xl"></div>
		<article class="relative p-4 sm:p-10 lg:p-20 bg-white dark:bg-gray-900 shadow-lg sm:rounded-3xl">
			<div class="mx-auto sm:max-w-md">
				<header class="w-full flex flex-nowrap justify-between items-center gap-8">
					<h1 class="w-full text-2xl font-semibold text-white text-center">¿Has olivdado tu password?</h1>
				</header>
				<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
					<%= render "devise/shared/error_messages", resource: resource %>
					<div class="py-8 text-base leading-6 space-y-4 text-gray-700 sm:text-lg sm:leading-7">
						<div class="rounded-md shadow-sm -space-y-px">
							<div>
								<%= f.email_field :email, autofocus: true, autocomplete: "email", class: "appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-t-md rounded-b-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: "Email address" %>
							</div>
						</div>
						<div class="relative flex flex-col justify-between items-center gap-12">
							<%= f.submit t('.send_instructions'), class: 'w-full flex justify-center items-center text-center bg-blue-500 text-white rounded-lg px-4 py-2 mx-2 gap-4 hover:cursor-pointer' %>
						</div>
						<div class="text-sm flex justify-between items-center gap-12 text-center">
              <%= link_to new_session_path(resource_name), class: 'w-full flex justify-between items-center text-center bg-blue-500 text-white rounded-lg py-2 px-4 gap-4 hover:cursor-pointer' do %>
								<span class="w-full"><%= t('.login')%></span>
								<%= inline_svg_tag 'icons/user/mood-cog.svg', class: 'w-6 h-6' %>
							<% end %>
							<%= link_to  new_registration_path(resource_name), class: 'w-full flex justify-between items-center text-center bg-blue-500 text-white rounded-lg py-2 px-4 gap-4 hover:cursor-pointer' do %>
								<span class="w-full"><%= t('.registration')%></span>
								<%= inline_svg_tag 'icons/user/user-up.svg', class: 'w-6 h-6' %>
							<% end %>
						</div>
					</div>
				<% end %>
			</div>
		</article>
	</section>
<% end %>
