<%= turbo_stream.update(
    'search_results',
    partial: 'admin/albums/search_results',
    locals: { albums: @albums }
    ) %>
<%= turbo_stream.replace 'count', UI::TitleCount.new(title: 'albums', count: @albums.count).call %>